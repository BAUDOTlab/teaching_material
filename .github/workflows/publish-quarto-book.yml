# Render Quarto to ./docs then deploy as static files (no Jekyll build of ./docs).
#
# GitHub repo → Settings → Pages:
#   Build and deployment → Source: "GitHub Actions"
#
# Do NOT use "Deploy from a branch" → /docs unless you commit ./docs to git.
# Do NOT use the default "Jekyll" starter workflow (it expects ./docs in the checkout).

name: Quarto Publish Book for best practices with Linux

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: "0 17 * * 5"
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: write
      pages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Quarto
        uses: quarto-dev/quarto-actions/setup@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tinytex: true

      # Quarto still probes for Rscript (knitr). Use distro R, not r-lib/setup-r,
      # which previously stalled for ~15 minutes.
      - name: Install Rscript
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends r-base-core

      # Mermaid → PNG for PDF hangs for hours if no Chrome/Edge is present
      # https://quarto.org/docs/authoring/diagrams.html
      - name: Install Chrome Headless Shell
        run: quarto install chrome-headless-shell --no-prompt

      # Markdown-only book: no R/downlit. `freeze: auto` reuses `_freeze/`
      # when sources are unchanged (https://quarto.org/docs/projects/code-execution.html#freeze).
      - name: Render book (HTML + PDF)
        run: quarto render

      - name: Publish to GitHub Pages
        if: github.event_name != 'pull_request'
        uses: quarto-dev/quarto-actions/publish@v2
        with:
          target: gh-pages
          render: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
